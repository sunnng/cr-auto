# CR Auto

本上下文定义《冲呀！饼干人：王国》AutoGo 自动化新项目(cr-auto)的 UI 层语言。本项目的 UI 层从 auto-cookie 迁移而来并自包含化：它不依赖任何领域包，只通过状态发布与命令两个接缝与宿主通信。

## Language

**控制面板**:
设备上以 ImGui 渲染、通过四个标签页(概览/任务/安全/识别)配置自动化行为的界面；它是 UI 自包含包 `internal/ui` 的对外主体。
_Avoid_: UI、界面、悬浮窗

**悬浮胶囊**:
游戏画面上可折叠的 ImGui 悬浮 HUD，带展开动画与指针吸附，供运行时快速查看状态和发出暂停/继续/停止操作。
_Avoid_: 悬浮球、Pill、HUD(太泛)

**截图隐身**:
截图前让 ImGui 遮罩连续渲染两帧全透明帧并握手确认，避免遮罩污染游戏截图。
_Avoid_: 隐藏遮罩、透明模式

**面板草稿**:
控制面板本地未保存的编辑状态，由用户点击保存后通过命令发出。
_Avoid_: 缓存、临时配置

**命令**:
面板对宿主发出的用户动作(保存/启动/暂停/恢复/停止/诊断/识别)，宿主通过命令处理器消费。
_Avoid_: 事件、回调
