# CR Auto

本上下文定义《冲呀！饼干人：王国》AutoGo 自动化新项目(cr-auto)的语言，分 UI 层与引擎/任务层两部分。UI 层从 auto-cookie 迁移而来并自包含化：它不依赖任何领域包，只通过状态发布与命令两个接缝与宿主通信；引擎/任务层按结构直译从懒人精灵 Lua 工程（帅斌饼干助手）迁移而来。

## Language

### UI 层

**控制面板**:
设备上以 ImGui 渲染、通过四个标签页(概览/任务/安全/识别)配置自动化行为的界面；它是 UI 自包含包 `internal/ui` 的对外主体。
_Avoid_: UI、界面、悬浮窗

**悬浮胶囊**:
游戏画面上可折叠的 ImGui 悬浮 HUD，带展开动画与指针吸附，供运行时快速查看状态和发出暂停/继续/停止操作。
_Avoid_: 悬浮球、Pill、HUD(太泛)
(口语与文档用"悬浮胶囊"；代码标识符沿用 `pill*` 前缀，与 auto-cookie 源码保持逐行对齐，方便未来迁移对比。)

**截图隐身**:
截图前让 ImGui 遮罩连续渲染两帧全透明帧并握手确认，避免遮罩污染游戏截图。
_Avoid_: 隐藏遮罩、透明模式

**面板草稿**:
控制面板本地未保存的编辑状态，由用户点击保存后通过命令发出。
_Avoid_: 缓存、临时配置

**命令**:
面板对宿主发出的用户动作(保存/启动/暂停/恢复/停止/退出脚本/诊断/识别)，宿主通过命令处理器消费。
_Avoid_: 事件、回调

**命令面**:
进程内 WebSocket，把中控发来的帧变成与控制面板、悬浮胶囊相同的 Command。不是控制面板，不是悬浮胶囊，也不是 apkctl HTTP 外壳。
_Avoid_: 卫星, 连接（单独当主键）, RPC

### 引擎与任务层

**结构直译**:
从懒人精灵 Lua 工程（帅斌饼干助手）向 Go 迁移游戏脚本代码的方式：保持 任务/会话/页面/路由/特征库 的职责分解与文件对应关系不变，逐模块翻译。
_Avoid_: 深度重设计、模块聚合、逐行翻译

**任务**:
注册到调度器的一个游戏功能单元，由开关、就绪检查和执行体构成；每个功能模块对应一个任务。
_Avoid_: 流程、脚本、动作序列

**会话**:
一个任务模块持有的跨轮持久进度（冷却、完成日期、累计停留等可恢复状态），存于本地存储，不依赖内存。
_Avoid_: 状态缓存、进度表、运行时状态

**页面**:
用多点比色特征标识的一个游戏画面，同时携带该画面内的按钮点击区域、找色定义与 OCR 区域。
_Avoid_: 界面、场景截图、状态

**路由**:
页面之间的导航步骤（如王国主城→矿山→勘查页），每步含目标页等待确认。
_Avoid_: 导航脚本、页面跳转

**特征库**:
一个模块的画面识别数据集合：多点比色特征、按钮区域、找色定义与 OCR 区域。
_Avoid_: 取色点集合、视觉规则、识别配置

**特征**:
特征库中的单条多点比色规格，即 `"x|y|color-偏移"` 串加相似度。
_Avoid_: 锚点、证据、色串

**图色**:
通过注入的 AutoGo 图色 API（多点比色 DetectsMultiColors、单点比色 CmpColor、多点找色 FindMultiColors）识别画面；特征库格式不变，由 color 门面转成 AutoGo 色串。
_Avoid_: 帧扫描比色、自研找色

**坐标库**:
某模块所需的固定按钮/入口区域集合（如岛屿入口、航海手册按钮）。
_Avoid_: 坐标表、点击点

**词条库**:
洗脆饼功能用于 OCR 匹配的装备词条名列表。
_Avoid_: 词条列表、文本库、词表

**守卫**:
主循环与等待分片期间按优先级扫描并自动处理的游戏内弹窗陷阱。
_Avoid_: 全局中断、弹窗处理器、Supervisor

**调度器**:
每轮按注册顺序串行执行到期任务的组件，条件不满足的任务本轮跳过。
_Avoid_: 任务队列、计划器、并发调度

**空闲提供者**:
注册到调度器的函数，返回某任务距离下次可运行的等待秒数，供主循环计算空闲等待时长。
_Avoid_: 唤醒计时器、冷却查询、定时器

**状态机**:
任务流程的执行器：按状态调用处理器，处理器返回下一状态名或 保持/重试/结束 符号。
_Avoid_: 流程图、行为树、状态表

**主循环**:
脚本永久运行引擎：每轮先扫守卫、再跑一轮调度，无任务时按空闲提供者结果等待。
_Avoid_: 主线程、游戏循环、事件循环
